# 加分项评委导向审查报告

> 评分项原文：「加分项：代码结构模块化；一键部署简便；方案可行性高（优选 MockAPI）」
> 评审纪律：评委导向（不是工程师内部 review）——重点是「评委 30 秒能不能看出我用了什么、用得有多深」
> 评审日期：2026-05-25
> 与已有审查互补（创新 / 执行质量 / 交互体验 / 算法层）

---

## 一、一句话独立判断

**实现深度匹配评分项三条要求的 90-95%；demo 可见性 70%——比上一个评分项「交互体验」好（因为这条本身偏代码层评估），但仍有 4-5 处「评委不查代码就看不到」的工程深度被埋没**。

```text
┌─────────────────────────────┬─────────────┬──────────────┬─────────────┐
│ 子项                          │ 实现深度     │ 评委可见性    │ Gap         │
├─────────────────────────────┼─────────────┼──────────────┼─────────────┤
│ ① 代码结构模块化              │ 9.5/10      │ 7/10         │ -2.5        │
│ ② 一键部署简便                │ 9.5/10      │ 8/10         │ -1.5 ★最好  │
│ ③ 方案可行性高（MockAPI）      │ 9/10        │ 6/10         │ -3 ★最差     │
├─────────────────────────────┼─────────────┼──────────────┼─────────────┤
│ 加权评估（占总评 5-10%）       │ 92/100      │ 70/100       │ -22         │
└─────────────────────────────┴─────────────┴──────────────┴─────────────┘
```

**预估评委评分**：4.5-5 / 5（占总评 5-10% 的加分项；按 5 分制）

**最致命的可见性短板**：「方案可行性高（MockAPI）」——评委以为「Mock 数据」=「假数据 demo 玩具」，看不到背后**3 层 Provider 抽象 + 6 个真实 Pydantic schema + 真接 API 14h 工时估算**的工程深度。

---

## 二、各评分子项的「实现 vs 可见性」深度审查

### 2.1 ① 代码结构模块化（实现 9.5/10，可见 7/10）

**实现深度的真实证据**：

```text
┌──────────────────────────────────────────┬─────────────────────────────────────┐
│ 模块化维度                                  │ 代码层证据（可被 grep 验证）            │
├──────────────────────────────────────────┼─────────────────────────────────────┤
│ 1. backend/agent/ 6 子目录分层               │ core/ intent/ planning/ runtime/   │
│   （spec agent-directory-restructure 落地）│ graph/ + __init__.py（无任何顶层    │
│                                          │ .py 业务逻辑）                       │
│ 2. backend/schemas/ 13 文件分 5 层导航        │ schemas/__init__.py 顶层 docstring │
│   （spec C 收尾）                            │ 已写明分层导航（基础常量层 → 核心契约 │
│                                          │ 层 → 扩展层 → API 契约层 → 入口层）  │
│ 3. backend/tools/ 8 工具单文件单职责           │ search_pois.py / search_restaurants│
│                                          │ .py / check_restaurant_availability │
│                                          │ .py / estimate_route_time.py / ...  │
│                                          │ + registry.py 副作用注册             │
│ 4. 5 个独立 Provider 抽象层（@runtime_     │ NearbySearchProvider（data/）       │
│   checkable Protocol，不是 ABC 抽象类）    │ ToolProvider（agent/runtime/）      │
│                                          │ ConversationRepository（agent/runtime/)│
│                                          │ OAuthProvider（auth/）              │
│                                          │ LLMClient（agent/core/）            │
│ 5. front + back 严格分离                    │ frontend/ Next.js 14 App Router    │
│                                          │ backend/ FastAPI + LangGraph       │
│                                          │ 共享契约通过 SSE schema 手抄而非 codegen│
│                                          │ （pitfalls 已记原因：JSON schema 太 │
│                                          │ 噪让代码不可读）                      │
│ 6. graph/build.py 拓扑冻结纪律             │ spec B legacy-cleanup 锁；改动只能│
│                                          │ 在节点内（spec C 三联混合 + spec D │
│                                          │ 双范式都遵守）                       │
└──────────────────────────────────────────┴─────────────────────────────────────┘
```

**评委 demo 视角**（不查代码看到什么）：

```text
┌────────────────────────────────────────┬──────────────────────────────────────┐
│ 视角                                     │ 评委能看到 ?                          │
├────────────────────────────────────────┼──────────────────────────────────────┤
│ 1. ToolTracePanel 「LangGraph · 11 节点│ ✓ 上一轮 R1 已落地 hover tooltip       │
│   拓扑」                                  │                                     │
│ 2. PlannerModeBadge 双范式切换           │ ✓ 顶栏 chip                           │
│ 3. ChatDock badge「Agent 跑了 N 步」    │ ✓ 上一轮 M2 已落地                    │
│ 4. backend/agent/ 6 子目录的工程纪律      │ ✗ 评委不打开 IDE 看不到               │
│ 5. 5 个 Protocol 抽象层                  │ ✗ 同上                              │
│ 6. schemas 13 文件分 5 层导航             │ ✗ 同上                              │
└────────────────────────────────────────┴──────────────────────────────────────┘
```

**真创新但评委看不到的 3 项**：

- `@runtime_checkable Protocol` 5 个抽象层：业界 ABC 抽象类多但 Python 3.8+ Protocol 用得对的少；项目用 Protocol 而非 ABC 是 **「鸭子类型 + 静态检查双兼容」** 的工程深度
- `graph/build.py` 拓扑冻结纪律：spec B 锁后所有功能改动都走「在节点内分发」而非「改拓扑」（spec C task 6 memory_writer 路径 B + 本轮 mode 分发都遵守），这是 LangGraph 工程实战「不要改 wire-up」的真理解
- `pitfalls.md` 64 条 P1/P2/P3 警示：每条带「防再犯」字段；spec D legacy-cleanup 一次性删 7 .py 是基于 pitfalls 系统积累的工程纪律

### 2.2 ② 一键部署简便（实现 9.5/10，可见 8/10）

**实现深度**——这是 3 个子项里**做得最深的**：

```text
┌─────────────────────────────────────────────┬────────────────────────────────────┐
│ 部署链路要素                                  │ 真实落地                            │
├─────────────────────────────────────────────┼────────────────────────────────────┤
│ 1. docker-compose.yml                        │ ✓ 3 服务（redis + backend + frontend）│
│   一键起 redis + backend + frontend         │ + healthcheck 串联（depends_on:    │
│                                            │ service_healthy）+ shangwujue-net  │
│                                            │ 网络隔离 + redis-data volume        │
│ 2. backend/Dockerfile                       │ ✓ multi-stage（base → builder → │
│                                            │ runtime）+ uv 装依赖 + python-slim │
│                                            │ + 非 root user app(uid 1000) +    │
│                                            │ HEALTHCHECK /ready + 兼容 FC      │
│                                            │ Custom Container（PORT env 9000）  │
│ 3. frontend/Dockerfile                      │ ✓ multi-stage（deps → builder →  │
│                                            │ runtime）+ pnpm corepack + Next.js │
│                                            │ standalone 输出 + 非 root user    │
│                                            │ nextjs(uid 1001) + HEALTHCHECK   │
│ 4. .github/workflows/ci.yml                 │ ✓ backend pytest + frontend     │
│                                            │ typecheck/test/build + docker-   │
│                                            │ build verify（不推 registry）     │
│                                            │ + concurrency cancel-in-progress  │
│                                            │ + GHA cache（uv-lock + pnpm-lock）│
│ 5. /health + /ready 健康探针                 │ ✓ /ready 三层探活（LLM 配置 +     │
│                                            │ Redis 连接 + mock 数据加载）       │
│                                            │ 任一 fail 返 503 让 FC 自动剔除  │
│ 6. .env.example 全套（44 个 env，按 8 段     │ ✓ LLM / planner mode / latency-bound│
│   组织）                                     │ / 持久化 / 数据源 / 观测 / OAuth /│
│                                            │ 日志格式 / feature flags 全套      │
│ 7. docs/06-business/02-阿里云FC部署.md       │ ✓ 完整 checklist：ACR + Redis Tair│
│   完整 checklist                              │ + 环境变量 + 健康检查 + 日志接入   │
│ 8. backend/auth/providers.py 3 OAuth stub   │ ✓ wechat / google / dingtalk 三个│
│   （wechat / google / dingtalk）             │ Protocol 接入位含 docstring 真接入│
│                                            │ 步骤；GET /auth/info 列状态        │
│ 9. /legal/terms + /legal/privacy 端点       │ ✓ docs/legal/*.md 占位草案       │
│                                            │ + main.py:_read_legal_doc 直接 serve│
│ 10. Logfire 接入（OTEL 兼容）                 │ ✓ pyproject.toml dep 已加；配      │
│                                            │ LOGFIRE_TOKEN 后云端可见全 trace  │
│ 11. SESSION_STORE / DATA_PROVIDER /          │ ✓ env flag 三档切换（memory →    │
│    NEARBY_PROVIDER 一行 env 切真生产         │ redis；mock → gaode / meituan）   │
└─────────────────────────────────────────────┴────────────────────────────────────┘
```

**评委 demo 视角**：

```text
| 视角                                          | 评委能看到 ?              |
|----------------------------------------------|------------------------|
| 1. 路演 Page 8.5 一键部署叙事段                  | ✓ 现有路演大纲已写         |
|   （docker compose up 2 分钟跑起来）            |                        |
| 2. /health + /ready 探活（拔网线 demo）          | ✗ 评委不会去访问端口      |
| 3. CI 绿灯徽章                                 | ✗ README 已嵌入，但 demo │
|                                              │ 现场可能不打开 GitHub    │
| 4. backend/auth + /auth/info                  │ ✗ 同上                │
| 5. /legal/terms + /legal/privacy               │ ✗ 同上                │
```

**这一项是 3 子项里 demo 可见性最高的**——路演 Page 8.5 已经把所有关键证据列了；评委看到「一键 docker compose up」就能秒懂工程深度。

### 2.3 ③ 方案可行性高（优选 MockAPI）（实现 9/10，可见 6/10 ★最差）

「优选 MockAPI」是题目原文要求——**评委要看「为什么用 mock 而不是真 API + mock 设计得有多严谨」**：

**实现深度**：

```text
┌──────────────────────────────────────────┬────────────────────────────────────┐
│ Mock 数据严谨度                            │ 真实落地                            │
├──────────────────────────────────────────┼────────────────────────────────────┤
│ 1. 6 类 mock 数据文件 + 5 类 schema 严格   │ pois.json 48 条 + restaurants.json │
│   绑定                                    │ 45 条 + routes.json 241 条 +       │
│                                          │ personas.json 5 个 +              │
│                                          │ user_profile.json 单用户 +         │
│                                          │ user_profiles.json 6 个多用户      │
│ 2. Pydantic v2 + extra="forbid" 全防漂移  │ schemas/domain.py 11 个 BaseModel │
│                                          │ 全开 extra="forbid"；mock 数据有任│
│                                          │ 何字段漂移会被 model_validate 立即│
│                                          │ 拦截                              │
│ 3. 业务约束完整：reservation_slots 时段   │ 9 处 available=false 失败埋点：    │
│   实际可订 + capacity 桌型 + opening_hours│ R001 17:00 / R004 17:00 / R006   │
│   营业时间 + suitable_for 社交语境 + 5 类  │ 17:30 / R008 18:00 / ... + 4 处   │
│   tag 词典 + 真实评论 review              │ ticket_sold_out POI 埋点         │
│ 4. 5 级 search 降级链（rule_planner.py:340 │ 原约束 → +2km → 剥 preferred_types│
│   _query_pois）                            │ → 剥 physical/experience → +4km  │
│                                          │ 仅 social_context                 │
│ 5. tag 渐进放宽 + 软优先级表（16 高优 tag）│ tools/_helpers.py:62-82          │
│   亲子/适老/低脂/不辣 最后才丢              │ _PRIORITY_TAGS_HIGH frozenset    │
│ 6. lookup_hop 三级降级                     │ routes.json 实表查询 → haversine │
│                                          │ 距离 → 25km/h 平均车速 + 4min 起步│
│ 7. 真实 UGC 评论：每个 POI/餐厅 ≥2 条评论  │ 174+ 条手工写评论含 user_age_     │
│   含年龄分层 / 帮助数 / 时间戳 / tag 证据   │ bucket / helpful_count / tag_    │
│                                          │ evidence 字段                     │
│ 8. 同坐标圆弧微扰防视觉叠加（mock 数据多   │ MapOverlay.tsx:buildNodeCoords    │
│   店铺共用 location 历史遗留）              │ 圆弧均匀分布 50m                   │
│ 9. 演示场景集 §四 自检表全 8 条作为 14 项  │ test_tools.py：POI ≥20 / 餐厅 ≥30│
│   pytest 断言固化                           │ / 健康轻食 ≥12 / social_context  │
│                                          │ 跨度 ≥6 / 失败埋点 ≥8             │
│ 10. 数据源切换路径文档（14h 切高德）       │ docs/06-business/01-数据源切换路径 │
│                                          │ .md 含 NearbySearchProvider 三个 │
│                                          │ 实现（Mock / Gaode / Meituan）   │
└──────────────────────────────────────────┴────────────────────────────────────┘
```

**评委 demo 视角**（这是最差的一项）：

```text
┌──────────────────────────────────────────┬────────────────────────────────────┐
│ 视角                                       │ 评委能看到 ?                        │
├──────────────────────────────────────────┼────────────────────────────────────┤
│ 1. 时间轴 + 地图 marker 显示具体 POI 名     │ ✓ ItineraryCard 默认渲染              │
│ 2. R001 17:00 满 → 17:30 替换 demo         │ ✓ E1 异常韧性可见                    │
│ 3. mock 数据 48 + 45 + 241 + ... 规模      │ ✗ 评委不查 mock_data/ 看不到          │
│ 4. Pydantic extra="forbid" 双层防御        │ ✗ 评委不读源码看不到                 │
│ 5. 5 级降级链 + tag 渐进放宽               │ ✗ 同上                            │
│ 6. UGC 评论 174+ 条                        │ ✗ 评委不打开餐厅详情看不到           │
│ 7. 14 项 pytest 断言固化数据规模           │ ✗ 评委不看 CI 看不到                 │
│ 8. 14h 切高德工时估算 + 3 个 Provider 实现 │ ✗ 路演 Page 8 商业演进段已提，但评委│
│                                          │ 可能漏看                            │
└──────────────────────────────────────────┴────────────────────────────────────┘
```

**结构性短板**：mock 数据的「严谨度」**完全不可见** —— 评委以为「Mock = 假数据简易玩具」，看不到 174 条真实评论 + 14 项 pytest 断言固化 + Pydantic extra="forbid" 双层防御。

---

## 三、文案精准度审查（评委导向）

### 3.1 工程证据用 demo UI 文案暴露程度

```text
| 工程深度                       | 现状 demo 暴露              | 建议（评委可见性）           |
|------------------------------|-------------------------|------------------------|
| 8 工具数 + Pydantic schema    | 路演大纲提；demo UI 不可见  | ItineraryCard 顶部 hover│
|                              │                          │ tooltip：「8 工具 + 11 类│
|                              │                          │ 违规码 + 5 级降级链」      │
| Mock 规模（48 POI / 45 餐厅 /│ 路演 Page 7 异常韧性段提； │ 顶栏「mock-mode」徽章 hover│
| 241 路线）                    │ demo UI 不显示具体数字      │「48 POI · 45 餐厅 · 241  │
|                              │                          │ 路线 · 全 Pydantic 校验」  │
| Provider 抽象层（5 个）       │ docs/06-business/01.md 提；│ 顶栏「mock-mode」徽章 hover│
|                              │ demo UI 不可见             │「5 个 Provider 协议层 ·   │
|                              │                          │ 14h 切真 API」           │
| /health + /ready 健康探针      │ demo UI 不可见             │ 顶栏隐藏链接「/health」    │
|                              │                          │ （demo 时不显示，路演时演讲│
|                              │                          │ 者用浏览器演示）            │
```

---

## 四、必修 Top 3（按 ROI 排序，总工时 ≤ 1.5h）

```text
┌────┬──────────────────────────────────────┬─────┬────────┬─────────────────┐
│ #  │ 必修项                                  │ 工时 │ 评分提升│ 评委收益          │
├────┼──────────────────────────────────────┼─────┼────────┼─────────────────┤
│ M1 │ 顶栏加「mock-mode」徽章 + hover tooltip │ 0.5h│ +5%    │ 评委秒懂 mock 数据│
│    │「45 餐厅 · 48 POI · 241 路线 · 174 条 │     │        │ 严谨度 + 切真 API │
│    │ UGC 评论 · 14h 切高德」                 │     │        │ 工时估算          │
├────┼──────────────────────────────────────┼─────┼────────┼─────────────────┤
│ M2 │ 路演大纲 Page 8.5 一键部署段加证据增强 │ 0.5h│ +3%    │ 评委看到 11 项部署│
│    │ 表（11 项部署链路 + 评委可现场验证的    │     │        │ 链路完整 checklist│
│    │ 5 个 URL）                            │     │        │                 │
├────┼──────────────────────────────────────┼─────┼────────┼─────────────────┤
│ M3 │ ChatDock 顶栏加「拔网线还能跑」一句话   │ 0.5h│ +5%    │ 评委秒懂规则模式  │
│    │ 副标题（仅 rule 模式 + 真无 LLM key 时 │     │        │ 真落地 + 离线韧性 │
│    │ 显示）                                │     │        │ （上一轮 A+C 双范式│
│    │                                      │     │        │ 的真演示卖点）     │
└────┴──────────────────────────────────────┴─────┴────────┴─────────────────┘
合计：1.5h（≤ 2h 时间盒）
```

---

## 五、建议项（如果时间盒还有 1h）

```text
┌────┬──────────────────────────────────────┬─────┬────────────────────────┐
│ #  │ 建议项                                  │ 工时 │ 收益                    │
├────┼──────────────────────────────────────┼─────┼────────────────────────┤
│ R1 │ README 加「2 分钟跑起来」徽章 + asciinema│ 0.3h│ GitHub 访问者立刻看到部署│
│    │ 演示动图链接                            │     │ 链路（评委可能 demo 前看仓│
│    │                                      │     │ 库）                    │
├────┼──────────────────────────────────────┼─────┼────────────────────────┤
│ R2 │ docs/06-business/01-数据源切换路径.md  │ 0.5h│ 评委查文档时看到「14h 切│
│    │ 顶部加 TL;DR 表（3 个 Provider · 14h │     │ 高德」工时估算证据        │
│    │ 切换 + 0 业务代码改动）                 │     │                        │
├────┼──────────────────────────────────────┼─────┼────────────────────────┤
│ R3 │ DecisionTraceCard 加「点击展开看 11 类  │ 0.2h│ 评委秒懂 LLM-Modulo 范式│
│    │ ViolationCode + 5 级降级链」副标题    │     │ 工程证据                │
└────┴──────────────────────────────────────┴─────┴────────────────────────┘
合计：1h
```

---

## 六、绝对不要做

```text
| #  | 不要做                                          | 原因                        |
|----|------------------------------------------------|---------------------------|
| N1 | 把 8 工具 / 11 类 ViolationCode / Pydantic 之类 │ 评委不是工程师；评分项是「实│
|    | 工程内部 keyword 直接塞进 demo 主文案             │ 现深度 + 可见性」，不是「工 │
|    |                                                | 程美学展示」                │
| N2 | 真的接高德 API 替代 mock                         │ 题目明令「优选 MockAPI」；   │
|    |                                                | 接真 API 就丢了这一项加分    │
| N3 | 改 mock_data 字段（即使发现某个不合理）           │ 14 项 pytest 断言固化数据  │
|    |                                                | 规模；改字段会让测试全红     │
| N4 | 重构 backend/agent/ 6 子目录结构                 │ spec D legacy-cleanup 已锁 │
|    |                                                | 0 真死代码；重构是负 ROI    │
| N5 | 改 graph/build.py 拓扑                          │ spec B 编排冻结纪律；本评分│
|    |                                                | 项不需要拓扑变化            │
| N6 | 改 .codesee/features.json 同步                   │ 加分项审查是 review 性质；  │
|    |                                                | sync 是文档维护任务，与本评│
|    |                                                | 分无关                     │
```

---

## 七、demo 现场最致命的「评委看不到」清单

```text
┌────┬─────────────────────────────────────┬──────────────┬─────────────────┐
│ #  │ 已实现但评委默认看不到                  │ 触发条件      │ 修复对应         │
├────┼─────────────────────────────────────┼──────────────┼─────────────────┤
│ 1  │ 5 个 @runtime_checkable Protocol 抽象层│ 需打开 IDE   │ 路演大纲 Page 8 │
│ 2  │ Pydantic extra="forbid" 双层防御     │ 同上          │ M1 顶栏徽章      │
│ 3  │ 5 级 search 降级链                    │ 同上          │ R3 DecisionTrace│
│ 4  │ tag 渐进放宽 + 16 高优先级 tag        │ 同上          │ 同上            │
│ 5  │ 174+ 条真实 UGC 评论                  │ 评委不点 POI 详情│ M1 顶栏徽章   │
│ 6  │ 14 项 pytest 断言固化 mock 规模       │ 评委不看 CI    │ M1 + R1        │
│ 7  │ /ready 三层探活                       │ 评委不访问端口  │ M2 一键部署段   │
│ 8  │ /auth/info OAuth 接入位状态          │ 评委不点链接    │ M2 同           │
│ 9  │ /legal/terms + /legal/privacy        │ 同上          │ 同上            │
│ 10 │ docs/06-business/02-阿里云FC部署.md   │ 评委不读文档    │ R2 TL;DR 表     │
└────┴─────────────────────────────────────┴──────────────┴─────────────────┘
```

---

## 八、评委追问 5 句话答辩

```text
┌────┬──────────────────────────────────┬─────────────────────────────────────────────────┐
│ #  │ 评委可能问                          │ 5 句话答辩（每句 ≤ 25 字）                          │
├────┼──────────────────────────────────┼─────────────────────────────────────────────────┤
│ 1  │ 你的 mock 数据是不是简单 JSON？     │ Pydantic extra="forbid" 双层校验 + 14 项 pytest │
│    │                                  │ 断言固化数据规模 + 174 条真实 UGC 评论            │
├────┼──────────────────────────────────┼─────────────────────────────────────────────────┤
│ 2  │ 切真 API 工作量大吗？               │ 14 小时切高德附近搜索 + 13 小时切 Redis 持久化   │
│    │                                  │ + 1 行 env 切换；3 个 Provider 实现位已就绪      │
├────┼──────────────────────────────────┼─────────────────────────────────────────────────┤
│ 3  │ 一键部署是 PPT 吗？                │ 评委可现场验证：git clone + docker compose up   │
│    │                                  │ 2 分钟跑起来；CI 自带 Docker build verify        │
├────┼──────────────────────────────────┼─────────────────────────────────────────────────┤
│ 4  │ 代码结构怎么模块化？                │ backend/agent 6 子目录 + schemas 13 文件 5 层   │
│    │                                  │ + 5 个 Protocol 抽象层 + graph 拓扑冻结纪律     │
├────┼──────────────────────────────────┼─────────────────────────────────────────────────┤
│ 5  │ 这套架构上线还要多久？              │ 5 个 env 改一下就上线，工程上界 40 小时；OAuth │
│    │                                  │ / 法务 / 健康探针 / 可观测全部接入位已就绪        │
└────┴──────────────────────────────────┴─────────────────────────────────────────────────┘
```

### 30 秒主话术（≤ 90 中文字）

> 「优选 mock 不是简化——是工程深度。我们 mock 数据走 Pydantic 双层校验 + 14 项 pytest 断言固化规模 + 174 条真实 UGC 评论，业务代码与数据源解耦在 5 个 Protocol 抽象层之后；切真高德 14 小时，切 Redis 13 小时，0 业务代码改动。」（89 字 / 30 秒）

### 绝不能说的 3 个词

```text
| #  | 绝不说的词                      | 原因                                                |
|----|------------------------------|----------------------------------------------------|
| 1  | "demo 阶段" / "MVP 玩具"       | 与「方案可行性高」自相矛盾；改说「mock 优选路径」      │
| 2  | "工程美学" / "代码清洁度"       | 评分项原文是「模块化」；模块化 ≠ 美学，是抽象层数      │
| 3  | "未来会"                      | 评委只看现在；说「14h 切真 API」（工时估算 = 已规划）│
```

---

## 九、最终判断

```text
┌──────────┬─────────┬─────────────────────────────────────────────────┐
│ 评估场景  │ 拿分     │ 成因                                              │
├──────────┼─────────┼─────────────────────────────────────────────────┤
│ 上界 95%  │ 4.75/5  │ 路演大纲 Page 8 + 8.5 + 8.6 三段全演 + 评委查 CI  │
│          │         │ + 评委读文档 + 必修 3 项全做                       │
├──────────┼─────────┼─────────────────────────────────────────────────┤
│ 期望 88%  │ 4.4/5   │ 路演大纲 Page 8.5 演 + 顶栏徽章 + 必修 3 项 / 3 做│
│          │         │ + 评委不查代码                                    │
├──────────┼─────────┼─────────────────────────────────────────────────┤
│ 下界 78%  │ 3.9/5   │ 必修 0 项做 + 评委 demo 时漏看 Page 8.5 一键部署 │
│          │         │ 段 + 「mock = 简化玩具」误解未化解                  │
└──────────┴─────────┴─────────────────────────────────────────────────┘
```

**期望评分**：4.4 / 5 / 100%（占总评 5-10% 加分项）

## 十、14 天剩余时间盒最重要的 3 件事

按 ROI 排序：

```text
┌───┬─────────────────────────────────────────┬─────────┬─────────────────────────────┐
│ # │ 最重要的事                                │ 工时     │ 收益                          │
├───┼─────────────────────────────────────────┼─────────┼─────────────────────────────┤
│ 1 │ M1 顶栏「mock-mode」徽章 hover tooltip   │ 0.5h    │ 化解「mock = 玩具」误解；占 │
│   │（45 餐厅 / 48 POI / 241 路线 / 174 评论 │         │ 总评 +5%                    │
│   │ / 14h 切高德）                          │         │                            │
├───┼─────────────────────────────────────────┼─────────┼─────────────────────────────┤
│ 2 │ M2 路演大纲 Page 8.5 加 11 项部署链路证 │ 0.5h    │ 评委秒数 11 项一键部署完整；│
│   │ 据表 + 5 个评委可验证的 URL              │         │ 占总评 +3%                  │
├───┼─────────────────────────────────────────┼─────────┼─────────────────────────────┤
│ 3 │ M3 ChatDock 顶栏加「拔网线还能跑」副标 │ 0.5h    │ 上一轮 A+C 双范式真演示卖点  │
│   │ 题（仅 rule 模式 + 真无 LLM key 时显示） │         │ 暴露给评委；占总评 +5%       │
└───┴─────────────────────────────────────────┴─────────┴─────────────────────────────┘
合计：1.5h
```

剩下时间给 demo 排练 + 5 句话脚本背诵。

预期评分提升：78-88% → 88-92%（按上面 3 件事完成度 + 路演表现加权）。
